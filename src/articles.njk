---
title: "Articles"
layout: "base.njk"
eleventyNavigation:
  key: Articles
  order: 1
---

<section>
  <header>
    <h1 class="f1 lh-title">Articles</h1>
  </header>

  {%- for item in collections.articles | reverse %}
  <article class="pv4 bb bw1 b--divider">
    <h2 class="f3 f2-m f2-l lh-title measure-narrow-ns mt0 mb2">
      <a href="{{ item.url }}">{{ item.data.title }}</a>
    </h2>

    <div class="mb2">
      <time class="f5 mid-gray db" datetime="{{ item.date }}">{{ item.date }}</time>
    </div>

    <div class="lh-copy measure-wide-m measure-wide-l">
      {{ item.excerpt }}
    </div>

    {%- if item.external %}
    <p>
      Article originally written for
      <span class="br-pill bw2 pv1 ph2 bg-mid-gray white dib">{{item.publication}}</span>
    </p>
    {%- endif %} {% if item.tags.size > 0 %}
    <div class="mt3">
      <b>Tagged with:</b> {{ item.tags | array_to_sentence_string }}
    </div>
    {%- endif %}
  </article>

  {%- endfor %}
</section>