---
layout: base
---
<article class="h-entry">
    <header class="mb3">
        <h1 class="f4 f3-l lh-title mt0 mb2 | p-name">{{ title }}</h1>
        <time class="ttu tracked f6 mid-gray dib | dt-published" datetime="{{ published | dateToISO }}">{{ date | readableDate('dd LLLL yyyy') }}</time>
        
         {# {% include location.html %} #}

    </header>



    <div class="content lh-copy measure-wide-m measure-wide-l | e-content ">

            <img loading="lazy" src="{{ image }}" class="u-photo" alt="{{ altText}}" />
        
            {% if content %}
                {{ content | safe }}
            {% endif %}

    </div>

    {% if tags %}
    <footer class="bt f5 b--divider">
        <div class="mv3">
            <b>Tagged:</b> {{ tags }}
        </div>
    </footer>
    {% endif %} 


    {# {% include post-navigation.html %} #}

</article>

